		//Задание:
		//Исправьте ошибки в коде.
        let user = {
            name: "admin",
            email: "admin@google.com",
        };

        console.log(user.name);
        console.log(user.email);

        user.email = "admin@nayax.com";

        console.log(user.email);